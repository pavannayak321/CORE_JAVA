package exceptionhandling;
/*
 * -->for every thread JVM will create a runtimeStack
 * 
 * -->all the method call performed by the  thread will be stored in stack is called "ACTIVATION RECORD"   or Stack Frame
 * 
 * -->after completing the all method call JVM will remove the activation record from the stack
 * 
 * -->after  completing all the method calls in stack  just before terminating the thread JVM will destroy the stack Object from memory
 */
public class RuntimeStackMechanism {

}
