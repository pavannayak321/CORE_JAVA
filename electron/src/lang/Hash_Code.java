package lang;
/*
 * for every object JVM will assign one unique id which is nothing but hashCode 
 * 
 * --->jvm uses the hashCode for saving the objects into HashTable,HashMap,HashSet
 * 
 * --->based on our requirement we can override the hashCode() method in our class 
 * 
 * --->if  we are not  overriding the hashCode() method then by-default Object class hashCode() method will be executed 
 * 
 * --->overriding the hashCode() method is said to be proper iff for every object we have to generate a unique number 
 */

public class Hash_Code {

	public static void main(String[] args) {
		

	}

}
